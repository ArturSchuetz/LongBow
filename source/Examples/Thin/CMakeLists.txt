# Examples on the thin interface: Vulkan and DirectX 12, authored in HLSL.
#
# Kept apart from the classic examples on purpose. Each tier is shown in its
# own idiom rather than one being made to look like the other.

add_subdirectory(01_HelloTriangle)
